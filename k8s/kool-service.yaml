apiVersion: v1
kind: Service
metadata:
  name: kool-service
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: kool-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 9376
